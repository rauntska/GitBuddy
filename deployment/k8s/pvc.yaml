apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: git-buddy-db-pvc
  namespace: git-buddy
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: default
