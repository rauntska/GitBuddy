apiVersion: v1
kind: Service
metadata:
  name: git-buddy-frontend
  namespace: git-buddy
spec:
  selector:
    app: git-buddy-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
